## 定义:
**内联函数不是在调用时发生控制转移,而是在编译时将函数嵌入在每个调用处**
## 特点:
**1.针对一些功能简单,规模较小又使用频繁的函数  
2.该函数一般不能有复杂的控制结构,例如switc,while  
3.inline只是个建议,编译器不一定会内联该函数;若是他觉得不复杂,才会内联;**  
**4.必须要与函数体一起才有效,只是与声明一起,编译器怎么会知道复杂不复杂呢,所以他会不理你;**
## 形式:
```
inline type function(type a)
{
    ...
}
```
